
name: Infrastructure Deployment
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  id-token: write
  contents: read

jobs:
  deploy-infra:
    uses: ./.github/workflows/terraform.yml
    secrets: inherit
    with:
      working-directory: './terraform'